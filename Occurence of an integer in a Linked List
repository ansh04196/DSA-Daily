class Solution {
  public:
    int count(struct Node* head, int key) {
        int count=0;
        while(head!=NULL)
        {
            if(key==head->data)
            {
                count++;
            }
            head=head->next;
        }
        return count;
    }
};
